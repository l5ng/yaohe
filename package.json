{
  "name": "yaohe-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "lint": "oxlint packages",
    "lint:knip": "knip",
    "typecheck": "pnpm -r build && tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "latest",
    "knip": "latest",
    "oxlint": "latest",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "latest",
    "vitest": "latest"
  }
}
